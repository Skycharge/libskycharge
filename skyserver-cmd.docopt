Skycharge charging pad server.

Usage:
    skycharged <addr> <port> --conffile <conff> [(--daemon (--pidfile <pidf>))]

Options:
    Skycharge server which executes commands received from TCP/IP network.
    Can be run as a service (daemon).
